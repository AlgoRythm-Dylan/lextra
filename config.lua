return {
    PROTECT_BUILTIN_LIBS = false, -- Disallow adding functionality to built-in tables like string and io
    AUTO_SEED_RANDOM = true -- math.randomseed(os.time()) when lextra.lua is required
}
